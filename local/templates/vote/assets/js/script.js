document.addEventListener('DOMContentLoaded', () => {
	// Весь код, расположенный здесь, будет выполнен сразу после готовности DOM

	// // Скрыть пройденные опросы
	// let voteCompleted = document.querySelectorAll(".vote-completed");
	// voteCompleted.forEach((vote) => {
	// 	vote.parentNode.remove();
	// });

});
